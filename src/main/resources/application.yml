server:
  port: 9000
spring:
  application:
  name: DemoLogin
  datasource:
    url: jdbc:mysql://localhost:3306/active_hub
    username: root
    password: abc123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
